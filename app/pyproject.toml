[tool.poetry]
name = "my-package"
version = "0.1.0"
description = ""
authors = ["Igor Soares, Luiz Felipe, Iury Rosal"]
readme = "README.md"
packages = [{include = "my", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
